# .yamllint.yml
extends: default

rules:
  document-start: disable
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ['true', 'false', 'yes', 'no']

ignore: |
  node_modules/
  vendor/
  _site/
